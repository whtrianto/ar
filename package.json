{
  "name": "web-ar-furniture",
  "version": "1.0.0",
  "description": "Web AR application for furniture visualization using WebXR",
  "private": true,
  "dependencies": {
    "@react-three/fiber": "^8.15.12",
    "@react-three/drei": "^9.88.13",
    "@react-three/xr": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "three": "^0.163.0",
    "web-vitals": "^2.1.4",
    "@google/model-viewer": "^3.3.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "homepage": ".",
  "scripts": {
    "start": "react-scripts start",
    "start:https": "set HTTPS=true && react-scripts start",
    "start:https:unix": "HTTPS=true react-scripts start",
    "build": "cross-env GENERATE_SOURCEMAP=false CI=false react-scripts build",
    "build:vercel": "cross-env GENERATE_SOURCEMAP=false CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "generate-cert": "node scripts/generate-cert.js"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
